#include <stdio.h>
#include <stdlib.h>
#include <string.h>
/**
 * main - Entry point
 * Return: Always (0) (Success)
 *
 */
int main(void)
{
	int n, i;

	n = num[i];
	for (i = 0; i < n; i++)
	{
	putchar (n % 10 + '0');
	}
	return (0);
}
