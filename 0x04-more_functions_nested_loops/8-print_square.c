#include <stdio.h>
#include "main.h"
/**
 * print_square - prints a sqaure of size size
 * @size: the size of the square
 * Return: void
 */
void print_square(int size)
{
	if (size < 0)
	{
		_putchar('\n');
		for (a = 0; b < (size); b++)
		{
			_putchar('#')
		}
		_putchar('\n');
	}
}
